describe('VirtualAccountTransaction', () => {

  //to execute only this test use only
  test.only('Balance is not sufficient', async () => {
    
  });

  //these tests will be excluded
  test('Balance is not sufficient', async () => {
    
  });

  test('Balance is not sufficient', async () => {
    
  });
})